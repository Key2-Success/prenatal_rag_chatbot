# Base URL of the FastAPI backend. Defaults to http://localhost:8000 if unset.
# The backend's CORS allow_origins is pinned to http://localhost:3000, which
# is the Next.js dev server's default — no extra config needed for local dev.
NEXT_PUBLIC_API_URL=http://localhost:8000
